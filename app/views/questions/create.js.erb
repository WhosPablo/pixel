$(".questions-list").prepend('<%=j render(@question, show_full_question: false) %>');
$(".question-form")[0].reset();

initializeCommentSlides();
CommentSetup();

questionLabels = {
  'suggested': [],
  'userCreated': []
};
